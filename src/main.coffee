q = (q)-> document.querySelector q
qa = (q)-> document.querySelectorAll q

# start requiring modules

# installed via npm, eg.
#$ = require 'jquery'

# as local files, eg.
lib = require './coffee/lib.coffee'
